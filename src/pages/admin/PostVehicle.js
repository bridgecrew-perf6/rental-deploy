import {Vehiclepostcomponent, Navigation, Layout} from "../../components";
import React from 'react';

const Postvehicle = () => {
    return (
        <Layout>
            <Navigation />
            <Vehiclepostcomponent/>
        </Layout>
    );
}

export default Postvehicle;
